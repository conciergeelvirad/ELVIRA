/**
 * Detail View Components
 *
 * Generic reusable components for displaying item details
 * across different entity types (Products, Amenities, Menu Items, etc.)
 */

export { ItemDetailView } from "./ItemDetailView";
export type { ItemDetailViewProps, ItemDetailField } from "./ItemDetailView";
